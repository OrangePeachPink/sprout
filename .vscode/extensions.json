{
  "// ": "Sprout recommends these so a contributor's editor helps from the first open (#110).",
  "recommendations": [
    "charliermarsh.ruff",
    "ms-python.python",
    "platformio.platformio-ide",
    "editorconfig.editorconfig",
    "davidanson.vscode-markdownlint",
    "tamasfe.even-better-toml",
    // Expert tier (#138) — match the repo's own tooling so the editor agrees with the gate:
    "github.vscode-pull-request-github",
    "github.vscode-github-actions",
    "streetsidesoftware.code-spell-checker",
    "ms-vscode-remote.remote-containers",
    "redhat.vscode-yaml",
    "ms-vscode.cpptools"
  ],
  // cpptools generically nudges toward CMake Tools as a companion regardless of build
  // system. This repo has no CMake project (firmware/platformio.ini is the actual build,
  // already covered by platformio-ide above) - installing it would be actively misleading,
  // pointing a contributor at `cmake`/`ctest` instead of `just build`/`pio test`.
  "unwantedRecommendations": [
    "ms-vscode.cmake-tools"
  ]
}
