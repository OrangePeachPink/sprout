# link_check allowlist — known-broken internal links, each tracked by an open ticket.
#
# One finding key per line, EXACTLY as link_check.py prints it: "<file> -> <target>".
# The gate skips these so it can go live over pre-existing debt without falsely blocking —
# while catching any NEW broken link immediately. Remove an entry the moment its fix lands
# (its ticket closes); the gate then enforces that it stays fixed. Nothing here is "ignore
# forever" — every line owes a ticket.

# --- #912 — 5 broken images on the served "Trust Your Sensor" design page (Design owns) ---
docs/design/onboarding/Sprout Trust Your Sensor.dc.html -> ../../evidence/sensor_photos/IMG_0529.JPEG
docs/design/onboarding/Sprout Trust Your Sensor.dc.html -> ../../evidence/sensor_photos/IMG_0527.JPEG
docs/design/onboarding/Sprout Trust Your Sensor.dc.html -> ../../evidence/sensor_photos/IMG_0526.JPEG
docs/design/onboarding/Sprout Trust Your Sensor.dc.html -> ../../evidence/sensor_photos/IMG_0537.JPEG
docs/design/onboarding/Sprout Trust Your Sensor.dc.html -> ../../evidence/sensor_photos/IMG_0528.JPEG

# --- #911 — internal-doc link sweep: ALL SIX findings FIXED (4 direct, 5 via the
#     bench-procedures repoint, 6/7 via #960, 8b via #955) — this section is empty
#     by design; the gate now enforces every one of them. ---
