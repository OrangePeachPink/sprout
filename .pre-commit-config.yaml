# .pre-commit-config.yaml — the single definition of "the checks" (ADR-0002 #10).
#
#   pre-commit install          # once: hooks run on every commit and fix what they can
#   pre-commit run --all-files  # on demand; this is exactly what CI runs
#
# Right-sized: format + lint + basic hygiene. No coverage / typing / security gates here
# (deliberately out of scope per ADR-0002 #10/#12). Frozen design provenance
# (docs/design/_archive) and the retired BACKLOG are left untouched.

exclude: '^(docs/design/_archive/|BACKLOG\.md$)'

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.20
    hooks:
      - id: ruff
        name: ruff (lint + autofix)
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=1024]

  # C/C++ formatting via clang-format (#120). Pinned to v22.1.5 — idempotency
  # verified: a second pass on any firmware file produces zero replacements.
  # Excludes docs/reference/ design-candidate C files (frozen provenance) in
  # addition to the global docs/design/_archive exclusion above.
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v22.1.5
    hooks:
      - id: clang-format
        types_or: [c, c++]
        exclude: '^docs/reference/'

  # markdownlint via the repo's own .markdownlint-cli2.jsonc (globs + ignores), so it
  # behaves identically to just / CI. Uses system npx (node is a standard dev tool).
  - repo: local
    hooks:
      - id: markdownlint-cli2
        name: markdownlint-cli2
        entry: npx --yes markdownlint-cli2
        language: system
        types: [markdown]
        pass_filenames: false

  # cspell — prose spelling, driven by the repo's own cspell.json dictionary (#109). Scoped to
  # Markdown (docs/design/** is cspell-ignored — Design's brand words). Same system-npx pattern
  # as markdownlint, so local == CI with no package.json. (Python/firmware scope is a later
  # step — checking code identifiers needs its own tuning to avoid false positives.)
  - repo: local
    hooks:
      - id: cspell
        name: cspell
        # Repo-wide on every commit (pass_filenames:false + always_run) so LOCAL scope == CI's
        # `pre-commit run --all-files`; pinned (#226) so the toolchain resolves the same version every run.
        entry: npx --yes cspell@10.0.1 lint --no-progress --no-summary --no-must-find-files "**/*.md"
        language: system
        pass_filenames: false
        always_run: true
